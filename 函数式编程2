### filter函数(参考案例filter.py）
- 过滤函数：对一组数据进行过滤，符合条件的数据生成一个新的列表并返回
- 跟map相比较：
      - 相同：都对列表偶的每一个元素逐一进行操作
      - 不同：
          - map会生成一个跟原数据相对应的新队列
          - filter不一定，制药符合条件的才会进入新的数据集合
      - filter函数语法：
          - 利用给定函数进行判断
          - 返回值一定是个布尔值
          - 调用格式：filter（f,data）,f是过滤函数，data是数据

### 高阶函数排序(参考函数sorted.py）
- 把一个序列按照给定算法进行排序
- key:在排序前对每一个元素进行key函数运算，可以理解成按照key函数定义的逻辑
      进行排序
- Python2跟3相差巨大



## 返回函数(参考案例返回函数.py）
- 函数可以返回具体的值
- 也可以返回一个函数作为结果

## 闭包(closure)（参考案例闭包.py和闭包坑.py）
- 当一个函数在内部定义函数，并且内部的函数应用了外部函数的参数或者局部变量，当
  内部函数被当做返回值的时候，相关惨呼和变量保存在返回的函数中，这种结果就叫闭包
- 上面定义的myF4是一个标准的闭包结构


### 出现的问题：
- 造成上述状况的原因是，返回函数引用了变量i,i并非立即执行，而是等到三个函数都返回
  的时候才统一使用，此时i已经变成3，最终调用的时候，都返回3*3
- 此问题描述成：返回闭包时，返回函数不能引用任何循环变量
- 解决方案：再创建一个函数，用该函数的参数绑定循环变量的当前值，无论该循环变量以后如何改变，已经绑定的函数参数值不在改变，


### 装饰器(decrator)(参考案例装饰器.py）
- 在不该懂函数代码的基础上无限制扩展函数功能的一种机制，本质上讲，
  装饰器是一个返回函数的高阶函数
- 装饰器的使用：使用@语法，即在每次扩展到函数定义前使用@+函数名


### 偏函数(参考案例偏函数.py）
- 参数固定的函数，相当于一个由特定参数的函数体
- functools.partial的作用是把一个函数某些函数固定，返回一个新函数
